# datadog_ref scripts 
- Is the weather endpoint ok? The page we are now on is just a ball of json
- datadog for listening to outgoing http calls: 
- A selinium script: 
- TDD datadog func! Essentially, script to use to port a DD to local env and write there and then copy-paste back into DD
